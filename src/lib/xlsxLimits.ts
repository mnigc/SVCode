/**
 * Preview cap for .xlsx sheets, shared by the parse worker (which clamps the
 * sheet range) and OfficeViewer (which shows the truncation banner).
 */
export const XLSX_MAX_ROWS = 500
export const XLSX_MAX_COLS = 40
