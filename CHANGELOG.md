# 更新日志

版本标题请保持 `## v<版本号>` 格式，发布工作流会按标签把对应小节抓取成 Release 说明与应用内「检查更新」的文案。

## v0.2.0

聚焦两件事：让「检查更新」真的能用，以及把索引的内存与 CPU 占用压下来。

### 自动更新

- 「检查更新」改用 Tauri 官方 updater，不再请求 GitHub API，之前点击就报 HTTP 403（接口限流）的问题消失。
- 安装包带 minisign 签名，下载后校验通过才安装，更新地址固定为 GitHub Releases 的 `latest.json`。
- 启动 3 秒后后台静默检查一次，24 小时内最多检查一次；发现新版本直接在「文件」菜单的「检查更新」右侧显示新版本号，不弹窗打断。
- 关于对话框里可直接下载并安装，显示下载进度；安装程序启动后应用自动退出，装完重新打开。

### 性能

- 全盘索引不再随文件增删持续增长：实测 156 万条条目稳定在约 234 MB，旧版会一路上涨。
- 索引压缩从「只打墓碑标记」改为真正合并，墓碑与尾部条目按比例回收。
- 删除操作合并成批量处理，并用前缀区间定位替代整段尾部线性扫描；同一负载下变更处理从约 8.8 秒降到约 4.2 秒。
- 只响应改名与增删事件，编辑器保存产生的内容写入不再触发索引更新。
- 目录遍历并行度封顶 8，索引线程优先级降到普通以下，前台输入不再被抢 CPU。
- 索引范围可在设置里选（当前目录 / 指定盘符 / 所有磁盘），不再强制全盘扫描。

> 首次升级需要手动安装一次本版本：0.1.1 没有内置签名更新器，之后即可在应用内直接更新。

## v0.1.1

- 新增 SMB 网络位置挂载、盘符右键菜单精简、菜单高亮保持。
- 新增设置面板（Ctrl+,）与界面对比度修复，附带 HTML 预览、快速访问、xlsx 后台解析。
